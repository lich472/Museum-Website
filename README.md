**To run the code:**
        1. Go to the Terminal
        2. Access the backend: type "cd ./backend"
        3. Run the code: type "npm run dev"

**To test the API backend:**
        1. Access the "example.http" file under "backend" folder
        2. Click on each "Send Request" to simulate the user request execution